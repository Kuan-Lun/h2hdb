\* Required green reachability/non-vacuity profile.  This deterministic,
\* weakly-fair witness is not an exhaustive safety proof; it is a
\* machine-enforced check that the cleanup chains remain reachable.
SPECIFICATION WitnessSpec

CONSTANTS
    Owners = {owner1, owner2}
    Stagings = {staging1}
    Headers = {header1}
    RequestBytes = {requestBytes1, requestBytes2}
    CommitRequestBytes = {requestBytes1}
    RequestDigests = {requestDigest1}
    Values = {sourceRootValue, value1}
    SourceRootValue = sourceRootValue
    Components = {fileComponent, tagComponent, directoryComponent, metadataComponent}
    FileComponent = fileComponent
    TagComponent = tagComponent
    DirectoryComponent = directoryComponent
    MetadataComponent = metadataComponent
    NoOwner = noOwner
    NoHeader = noHeader
    NoValue = noValue
    NoObservation = noObservation
    NoReplayFingerprint = noReplayFingerprint
    SeedObservation = 0
    MaxTime = 1
    LeaseSpan = 1
    MaxOuterGeneration = 2
    MaxClaimGeneration = 2
    MaxObservation = 1
    MaxPagePosition = 0
    MetadataUnits = 1

PROPERTY WitnessTargetReached
